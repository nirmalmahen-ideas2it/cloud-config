spring:
  cloud:
    datasource:
      type: com.zaxxer.hikari.HikariDataSource
      url: jdbc:postgresql://localhost:5432/myFirstMicroService
      username: myFirstMicroService
      password:
      hikari:
        poolName: Hikari
        auto-commit: false