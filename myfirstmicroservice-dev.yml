

server:
  port: 8082

custom:
  message: "Hello from config server - DEV profile"

management:
  endpoint:
    health:
      validate-group-membership: false  # Disable validation for health groups

